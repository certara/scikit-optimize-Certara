[build-system]
# Minimum requirements for the build system to execute.
requires = [
    "setuptools",
    "wheel",
    "numpy>=2.0.0",
    "scipy>=1.15.2",
    "scikit-learn>=1.5.1",
    "pyaml>=16.9",
    "joblib>=0.11",
]